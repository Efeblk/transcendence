# Start with the official PostgreSQL image
FROM postgres:13

# Set environment variables (if needed)
ENV POSTGRES_DB=pingpongdb
ENV POSTGRES_USER=postgres
ENV POSTGRES_PASSWORD=yourpassword

# If you have custom SQL scripts or configurations to add, copy them here
# COPY custom.sql /docker-entrypoint-initdb.d/

# Expose the PostgreSQL port
EXPOSE 5432

# Optional: You can add custom commands or scripts if needed
